---
date: 2017-03-28 11:03
status: public
title: 跟随《R语言编程艺术》敲代码
---

希望这个记录能坚持打卡。
###2017-03-28 
在R中新建一个向量，如x<-c(4,6,7)。使用索引来表示向量中的某个值，可以知道x[1]为4，x[2]为6，x[3]为7。输入x[4]，提示NA，可以理解；但输入x[0]，输出的却是numeric(0)？
当调用R自带的数据集时（比如Nile），输入data(Nile)后，Rstudio工作环境中会出现该数据，属性标为<Promise>(unevaluated promise)，但在控制台输入Nile后，属性会变为其真正属性Time-Series
函数中的全局变量和局部变量的区别？
###2017-03-31
矩阵乘法。大概百度了一下，还是不太明白矩阵*多元素向量之后得到的算是什么类型。R的结果是以某列（矩阵？）表示。
死板的教科书说，对象是类的实例。所以，类到底是什么东西？根据oracle的Java教程，
A class is the blueprint from which individual objects are created.
可以理解为“类”是创建对象的一个模板。
打个比方，建筑有很多“类型”，如住宅、商用、公共建筑；又如多层、高层；但每栋建筑有可能有相同货不同的建造施工方案（“类”）。